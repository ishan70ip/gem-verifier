# ML stack for the evidence-finder (Colab / production).
# The service (app.py) runs WITHOUT these, using its TF-IDF fallback.
# Install for training + SBERT inference:
sentence-transformers>=3.0
torch --index-url https://download.pytorch.org/whl/cpu
onnxruntime
onnx
numpy
